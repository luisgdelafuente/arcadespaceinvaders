# Static site — no build step. Explicit base/publish so these settings win
# over any stale "Base directory" configured in the Netlify site UI.
[build]
  base = "/"
  publish = "/"
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
